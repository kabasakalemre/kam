{
  "env": {
    "credentials": {
      "valid": {
        "username": "test.admin@getir.com",
        "password": "the-password"
      },
      "invalid": {
        "username": "invalid_username@test.com",
        "password": "invalid_password"
      }
    }
  }
}